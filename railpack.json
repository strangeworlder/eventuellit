{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        { "cmd": "mkdir -p /app/node_modules/.cache" },
        { "cmd": "npm install" }
      ]
    }
  }
}
